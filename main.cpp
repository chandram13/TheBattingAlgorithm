// Marvish Chandra
// The Batting Algorithm

#include <iostream>

// each hit represents 1 inning
class baseball{
    public static void teamOne(String[] n,s1,s2,s3,f1,f2,f3,x,y){
        totalHits = ((1/9 * n) * 1/4 + (s1)^x + (f1)^y) + ((1/9 * n) * 1/4 + (s2)^x + (f2)^y) + ((1/9 * n) * 1/4 + (s3)^x + (f3)^y) = 9
        cout << "The total hits for the first baseball team is: " + cout << totalHits;
        return 0;
    }
    public static void teamTwo(String[] n,s1,s2,s3,f1,f2,f3,x,y){
        2nd_totalHits = ((1/9 * n) * 1/4 + (s1)^x+ (f1)^y) + ((1/9 * n) * 1/4 + (s2)^x + (f2)^y) + ((1/9 * n) * 1/4 + (s3)^x + (f3)^y) = 9
        cout << "The total hits for the second baseball team is: " + cout << 2nd_totalHits;
        return 0;
    }
}